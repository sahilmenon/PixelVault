# Core
numpy>=1.24
opencv-python>=4.8
tqdm>=4.65
reedsolo>=1.7  # Reed-Solomon ECC for --ecc flag
cryptography>=41.0  # AES-256-GCM encryption for --password flag

# YouTube download
yt-dlp>=2024.1.1

# YouTube upload (optional — only needed for --upload / upload subcommand)
google-api-python-client>=2.100
google-auth-oauthlib>=1.1
google-auth-httplib2>=0.1
